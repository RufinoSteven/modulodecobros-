# This is where the user should add their credentials for signature.
# Credentials must be encrypted using the same mechanism as dbsettings.properties.
# The loader will decrypt them at runtime.
signature.username=
signature.password=